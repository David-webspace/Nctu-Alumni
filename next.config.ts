import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['i.imgur.com', 'image.pexels.com', 'images.unsplash.com', 'images.pexels.com', 'example.com', 'cdn.pixabay.com', 'nctualumni-images.s3.ap-northeast-1.amazonaws.com'],
  },
};

export default nextConfig;
